# =============================================================================
#  后端镜像构建上下文忽略规则
#
#  为什么需要：docker compose 的 backend 服务以 ./backend 为构建上下文。
#  不加这个文件时，docker 客户端会把整个 backend/ 目录（含 target/，
#  本项目实测约 86~164MB）全部打包上传给 daemon，每次构建都要白等这些 IO。
#  Dockerfile 实际只需要 pom.xml 与 src/。
# =============================================================================

# 构建产物
target/
*.jar
*.war
*.class

# 日志与临时文件
*.log
logs/
*.tmp
*.bak

# 本地环境变量（可能含密钥）
.env
.env.local

# IDE
.idea/
*.iml
.vscode/

# 操作系统
.DS_Store
Thumbs.db
